import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      default: "#f7f7f7",
    },
    primary: {
      main: "#4f46e5",
    },
  },
  shape: {
    borderRadius: 12,
  },
});